pipeline {
    agent { docker { image 'python:3.8.1' } }
    stages {
        stage('Test') {
            steps {
                withCredentials([string(credentials: 'credTelegram', variable: 'TELEGRAM')]) {
                    sh 'python ./hello_world.py --data $TELEGRAM'
                }
            }
        }
    }
}
