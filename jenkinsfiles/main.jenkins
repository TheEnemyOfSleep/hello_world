pipeline {
    agent { docker { image 'python:3.8.1' } }
    parameters {
        password(name: 'DATA', defaultValue: '', description: 'Data')
    }
    stages {
        stage('Test') {
            steps {
                sh """
                python ./hello_world.py --data ${params.DATA}
                """
            }
        }
    }
}
